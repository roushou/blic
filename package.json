{
  "name": "boune-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "release": "bun scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.17.0",
    "typescript": "^5"
  }
}
